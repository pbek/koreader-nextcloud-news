local _ = require("gettext")
return {
    name = "nextcloud_news",
    fullname = _("Nextcloud News"),
    description = _([[Synchronizes articles from a Nextcloud News server and saves them as EPUB files, with two-way read/starred state sync.]]),
}
